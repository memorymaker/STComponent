! Relation 과 Graphic 의 포커스 관련 확인 필요


Work

	미니맵 반투명 처리?

	확대 축소 시 

	[완]Relation 도착지 화살표 크기 조정

	[완]NODE_DETAIL_ID, NODE_DETAIL_SEQ 수정중
	[완]각 노드 X(닫기 버튼) 색상 변경
	[완]각 node 범위 넘어갈 때 처리

	[완]일반 컬럼도 table alis가 표시되도록 수정

	[완]툴팁 수정중 / 툴팁 포멧을 컬럼 별로 지정할 수 있는 기능
	[완]JOIN 조건을 FIELD TO TABLE로 추가할 수 있는 기능

	[완]릴레이션 툴팁 구현
	[완]column1, column2 or table, join type, value 등 정보 표시

	[완]노드의 width가 작을 때 아이디가 타이틀바를 넘어갈 때 처리

Error
	"메모리가 부족합니다" 해결 필요
		System.OutOfMemoryException

	[완]노드 아이디 편집중 확대 축소해도 텍스트 박스가 남아있음
		!완 원격이 아닌 화면에서 확인 필요

	[완]Node 추가, 삭제 시 미니맵에 바로 반영되지 않음

	[완]메모 노드 사이즈 조절 시 텍스트 정렬 되지 않음
		포커스가 있을 때 사이즈 조절 불가능

	node OriginalLeft, Top, Width, Height 처리?

	[완]컬럼 노드에서 테이블 노드로 드래그 됨

	[완]노트 추가 시 미니맵에 바로 반영되지 않음
	[완]노드 추가 시 확대, 축소 상태면 부정확한 위치에서 생성됨
	
	[완]확대 축소 상태에서 이미지 저장 테스트

	[완]확대 축소 시 컨트롤 위치가 잘못된 곳으로 이동됨.
	[완]컬럼 노드 SetAutoSize
	

DataModeler
	SetData, GetData 구현
	

테이블 노드
	...
	[완]툴팁 구현

릴레이션 노드
	...

마우스 우클릭 툴팁 메뉴 추가
	[완]일반(메모 노드 추가, 컬럼 노드 추가)

	[완]릴레이션(수정, 삭제)
		[완]Inner, Left, Right 는 각각의 릴레이션이 아닌 엔티티와의 관계이므로
		[완]릴레이션 속성 수정이 아닌 다른 방법이 필요함.
		[완](하나를 바꾸면 해당 관계의 릴레이션은 전부 적용, 편집 창에서 다수의 관계 표현)

		[완]릴레이션 우클릭 시
			[완]선택된 노드를 클릭하면 릴레이션들의 Status 변경 없이 툴팁 띄움
			[완]선택되지 않은 노르를 클릭하면 해당 릴레이션 선택(다른 릴레이션 none) 후 툴팁
		[X]수정 시 다중 선택일 경우
			[X]모두 동일한 릴레이션 타입이면 셀렉트 박스에 해당 타입 표시
			[X]다른 타입이면 셀렉트 박스에 - 표시

컬럼 노드 구현
	[완]컬럼 아이디 편집 구현
		[완]텍스트 창에서 esc, enter 키 누를 때 효과음 안 나오도록
		[완]에디터 완료될 때 Event 필요


메모 노드 구현
	[완]컨트롤에 GetImage() 추가?(해당 이미지를 받은 후 메모노드에서 draw하기 위해)
	[완]테이블 노드 또는 컬럼 노드에서 드래그로 컬럼 값을 입력 할 수 있는 기능 추가

	



기타 컨트롤
	GraphicListView
		ColumnHeight 의 수치가 font의 height보다 작을 때 텍스트가 범위를 넘어감

기획
	[다른 기능으로 대체]join 에서 기본적으로 들어가는 and 의 구현 방안
		Relation Editor에서 Condition 추가?
			Condition은 from 과 to 의 필드 선택 후 텍스트{0} 사용?
	-> 템플릿 자동 변경으로 처리(최초 추가 join 조건을 사용자가 입력하고 템플릿을 적용할 때
	   해당 조건으로 자동으로 추가 처리)

템플릿 공유
	공유 타입
		sql cs java 단일이거나 조합?